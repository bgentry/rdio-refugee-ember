import { momentDurationFormat } from '../../../helpers/moment-duration-format';
import { module, test } from 'qunit';

module('Unit | Helper | moment duration format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentDurationFormat(42);
  assert.ok(result);
});
